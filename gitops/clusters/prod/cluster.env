GITOPS_REPO_URL=https://github.com/panagiod/infra
# Production tracks immutable release tags (see docs/delivery/release-pipeline.md).
# Updated by .github/workflows/release.yml on first release — do not set to main for real prod.
GITOPS_TARGET_REVISION=main
GATEWAY_HOSTNAME=prod.gateway.example.com
