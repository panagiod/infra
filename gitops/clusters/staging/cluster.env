# Per-cluster GitOps settings — consumed by Kustomize replacements in kustomization.yaml
GITOPS_REPO_URL=https://github.com/panagiod/infra
GITOPS_TARGET_REVISION=main
GATEWAY_HOSTNAME=staging.gateway.example.com
